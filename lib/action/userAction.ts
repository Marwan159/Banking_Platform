"use server";

export const signUp = async (uesrData: SignUpParams) => {
  try {
  } catch (error) {
    console.log(error);
  }
};

export const signIn = async (userData: signInProps) => {
  try {
  } catch (error) {
    console.log(error);
  }
};
